.next
node_modules
tsconfig.tsbuildinfo
tsconfig.check.tsbuildinfo

# Secrets — must never be committed
.env
.env.*
.env.local
.env.*.local

# Agent wallet keystores — these contain PLAINTEXT private keys.
# NEVER commit a file that matches this pattern.
.wallets.json
*.wallets.json
wallets.json

# Vercel project config (contains project/org IDs)
.vercel

# Compiled contract artifacts are huge and re-generated by
# `pnpm contracts:compile`. Keep them out of git to avoid noise.
artifacts

# NOTE: `deployments/` IS checked in — evaluators need
# `deployments/xlayer-196.json` to verify the live contract addresses.
# NOTE: `submission/` IS checked in — evaluators need FINAL_PACKET.md
# and final-packet.json as part of the Build X Season 2 hand-off.
